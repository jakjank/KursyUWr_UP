#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main(){
    cout.precision(20);
    int n;
    cin>>n;
    vector<int> t[n];

    while(n--){
        int i, j;
        cin>>i>>j;
        t[i].push_back(j);
    }

    vector<int> T[n];
    for()


    return 0;
}